
import 'package:flutter/material.dart';

class DesiredColor extends StatelessWidget {
  const DesiredColor({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
